# Wallet Example

Barebones React SDK wallet demo: create account, show balances, claim notes, send tokens.

## Run

```bash
cd ../..
yarn install
yarn build
cd examples/wallet
yarn install
yarn dev
```
