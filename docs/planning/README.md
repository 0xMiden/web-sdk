# Planning docs

Working / design docs imported from `0xMiden/miden-client` during the web/WASM split (per @SantiagoPittella's review on miden-client #1992 — these belong with the web SDK code, not the Rust client).

| File | Notes |
|---|---|
| `AGENTS.md` | Repo-wide agent guidance for working on web SDK code |
| `Api.Rework.Impl.md` | Implementation notes on the simplified API rework |
| `REACT_SDK_PLAN.md` | Roadmap / open questions for the React SDK |
| `SimplifiedAPI.md` | Design doc for the high-level `MidenClient` API surface |
| `SimplifiedAPI.sdk.review.md` | Review notes / follow-ups on the same |

These are snapshots — not actively maintained. Convert to GitHub issues when an item becomes a tracked piece of work.
